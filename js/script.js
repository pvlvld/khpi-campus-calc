// https://github.com/microsoft/calculator/raw/refs/heads/main/src/Calculator/Assets/CalculatorIcons.ttf
// Icons Font

import {App} from "./app.js";

// Calculator supports input from the keyboard as well as via mouse.
// (there is brackets, btw, but i was too lazy to add them to the screen keyboard)

const app = new App();
app.init();
